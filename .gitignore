

# dependencies
node_modules/
.pnp
.pnp.*
.yarn/*

# next.js
.next/
out/
build/
dist/

# production
*.local

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env
.env.local
.env.*.local

# typescript
*.tsbuildinfo

# catalog images built from owner PDFs — regenerated locally via
# scripts/build-catalog-images.ts, not tracked
public/catalog/
next-env.d.ts

# vercel
.vercel

# ide
.vscode/
.idea/
.DS_Store
Thumbs.db

# testing
coverage/
playwright-report/
test-results/

# local logs
*.log

# verification screenshots (root only)
/*.png
tests/e2e/.auth/
pre-wipe.dump

# Catalog extraction artefacts — regenerable from source PDFs via the
# accompanying scripts. Pages are ~200 MB (rendered @ 200dpi); swatches
# and crops are already copied into public/catalog/uploads/ at import
# time, so the intermediates aren't needed in-repo.
scripts/fedora-pages/
scripts/fedora-swatches/
scripts/rugway-pages/
scripts/rugway-crops/
scripts/rugway-crops-hi/
scripts/rugway-crops-test/
scripts/rugway-quality/
scripts/rugway-quality-test/
