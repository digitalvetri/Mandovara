// Mandovara ships goods to site with an installer, not via a dispatch/challan.
// The install flow lives under /install. This stub satisfies existing imports.
export function DispatchForm(_props: { orderId: string; lines: unknown[] }) {
  return null;
}
